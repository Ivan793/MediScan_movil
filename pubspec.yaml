name: mediscan_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.3.6  # Para subir imágenes
  image_picker: ^1.0.7       # Para seleccionar imágenes
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10
  getwidget: ^7.0.0
  get: ^4.6.6
  http: ^1.2.1   # <---- AGREGADO

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
